# This file generates the GitHub Actions matrix.
# For simple board + shield combinations, add them to the top level board and
# shield arrays, for more control, add individual board + shield combinations
# to the `include` property. You can also use the `cmake-args` property to
# pass flags to the build command, `snippet` to add a Zephyr snippet, and
# `artifact-name` to assign a name to distinguish build outputs from each other:
#
# board: [ "nice_nano" ]
# shield: [ "corne_left", "corne_right" ]
# include:
#   - board: bdn9_rev2
#   - board: nice_nano
#     shield: reviung41
#   - board: nice_nano
#     shield: corne_left
#     snippet: studio-rpc-usb-uart
#     cmake-args: -DCONFIG_ZMK_STUDIO=y
#     artifact-name: corne_left_with_studio
#
---
include:
  - board: nice_nano
    shield: simple34_left_central
    artifact-name: simple34_left_central
  - board: nice_nano
    shield: simple34_left
    artifact-name: simple34_left_peripheral
  - board: nice_nano
    shield: simple34_right
    artifact-name: simple34_right_peripheral
  - board: efogtech_dongle_1k
    shield: simple34_dongle
    snippet: nrf52833-nosd
    cmake-args: -DCONFIG_ZMK_SPLIT_ROLE_CENTRAL=y
  - board: nice_nano
    shield: settings_reset
    artifact-name: settings_reset_nice_nano
  - board: efogtech_dongle_1k
    shield: settings_reset
    snippet: nrf52833-nosd
    artifact-name: settings_reset_dongle
